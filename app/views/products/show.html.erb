<div class="container">

  <div class="row">
    <div class="col">
      <h2><strong><%= @product.name %></strong></h2>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <p>By: <%= link_to @product.merchant.username, merchant_path(@product.merchant_id) %></p>
    </div>
    <div class="col">
      <h3>Product Reviews</h3>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <%= link_to image_tag("#{@product.image}", :alt => 'product-img', :class => 'image-tag'), product_path(@product.id) %>
    </div>
    <div class="col">
      <% @product.reviews.each do |review| %>
      <h5><%= review.rating %> out of 5</h5>
      <% end %>
      <div>
        <% @product.reviews.each do |review| %>
        <p>"<%= review.description %>"</p>
        <p><%= review.created_at.strftime("%B %d, %Y") %></p>
        <% end %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <p><%= @product.description %></p>
    </div>
    <div class="col">
      <% @product.reviews.each do |review| %>
      <p>"<%= review.description %>"</p>
      <p><%= review.created_at.strftime("%B %d, %Y") %></p>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <p><strong>Price: </strong>
        <%= number_to_currency(@product.price) %>
      </p>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <section>
        <h3><%= button_to "Add to Cart", root_path %></h3>
      </section>
    </div>
  </div>
</div>
